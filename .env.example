# Salla OAuth app credentials (set in Vercel Project Settings -> Environment Variables)
SALLA_CLIENT_ID=
SALLA_CLIENT_SECRET=
# Must exactly match the callback URL configured in Salla Partner Portal
SALLA_REDIRECT_URI=https://your-project.vercel.app/
